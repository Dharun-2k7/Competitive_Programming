#include<bits/stdc++.h>
using namespace std;
#define vi vector<int>
int main(){
    int n; cin>>n;
    vi a(n), b(n);
    for(int i=0; i<n; i++) cin >> a[i];
    for(int j=0; j<n; j++)  cin>> b[j];
    reverse(b.begin(), b.end());
    
   
        
    
}