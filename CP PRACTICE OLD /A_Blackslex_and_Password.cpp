#include <bits/stdc++.h>
using namespace std;
using ll =long long;
int main() {
    int t; cin >> t;
    while (t--) {
        int k, x;
        cin >> k >> x;
        cout << (ll)k * x + 1 << endl;
    }
}
